<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teste">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Combustivel"></activity>
        <activity android:name=".Componentes" />
        <activity
            android:name=".Netflix"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Noticia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Jokenpo"
            android:label="@string/activity_jokenpo" />
        <activity android:name=".Frases" />
        <activity android:name=".Sorteio" />
        <activity android:name=".MainActivity"> <!-- android:screenOrientation="portrait" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>